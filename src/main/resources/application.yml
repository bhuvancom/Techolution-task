server:
  port: 8001
spring:
  application:
    name: techolution-poc
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
    database:
        platform: h2
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true